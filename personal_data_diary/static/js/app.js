const $=s=>document.querySelector(s),d=window.INITIAL_DATA;$('#days').textContent=d.quality.days_recorded+'/7';$('#metrics').textContent=d.quality.metrics;$('#missing').textContent=d.quality.missing_values;const max=Math.max(...d.rows.map(r=>Number(r.steps)));$('#chart').innerHTML=d.rows.map(r=>`<div class="bar-wrap" title="${r.day}: ${r.steps} steps"><div class="bar" style="height:${r.steps/max*100}%"></div></div>`).join('');$('#labels').innerHTML=d.rows.map(r=>`<span>${r.day}</span>`).join('');$('#insights').innerHTML=d.insights.map(x=>`<div class="insight"><div class="insight-icon">✓</div><div><p>${x}</p></div></div>`).join('');$('#table').innerHTML=`<table><thead><tr>${Object.keys(d.rows[0]).map(k=>`<th>${k}</th>`).join('')}</tr></thead><tbody>${d.rows.map(r=>`<tr>${Object.values(r).map(v=>`<td>${v}</td>`).join('')}</tr>`).join('')}</tbody></table>`;