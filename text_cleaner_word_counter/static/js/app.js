const $ = (s) => document.querySelector(s); const sample = 'Data thinking is a habit, not a one-time task. Clean inputs create clearer decisions.';
async function analyze() { const text = $('#raw-text').value; const res = await fetch('/api/analyze',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({text,lowercase:true,remove_punctuation:true,collapse_spaces:true})}); const data = await res.json(); $('#words').textContent = data.words; $('#characters').textContent = data.characters; $('#cleaned').textContent = data.cleaned || 'Nothing to show yet.'; $('#signals').innerHTML = `<div class="metric"><div class="metric-label">Sentences</div><div class="metric-value">${data.sentences}</div><div class="metric-change">Structure count</div></div><div class="metric"><div class="metric-label">Read time</div><div class="metric-value">${data.reading_minutes}m</div><div class="metric-change">At 220 words/min</div></div><div class="metric"><div class="metric-label">Top words</div><div class="metric-value" style="font-size:18px">${data.top_words.map(item=>item[0]).join(', ') || '—'}</div><div class="metric-change">Most repeated terms</div></div>`; }
$('#analyze').addEventListener('click', analyze); $('#sample').addEventListener('click', () => { $('#raw-text').value = sample; analyze(); });
