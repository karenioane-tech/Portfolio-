const d=window.INITIAL_DATA,$=s=>document.querySelector(s);$('#rows').textContent=d.quality.clean_rows;$('#score').textContent=Math.round(d.summary.avg_score).toLocaleString();$('#completion').textContent='100%';const vals=d.rows.map(r=>Number(r.body_mass_g)),m=Math.max(...vals);$('#chart').innerHTML=vals.slice(0,30).map(v=>`<div class="bar-wrap"><div class="bar" style="height:${v/m*100}%"></div></div>`).join('');$('#labels').innerHTML=d.rows.slice(0,30).map((_,i)=>`<span>${i+1}</span>`).join('');$('#correlations').innerHTML=d.correlations.map(x=>`<div class="list-item"><div><h3>${x.pair}</h3><p>${x.value}</p></div></div>`).join('');$('#groups').innerHTML=Object.entries(d.groups).map(([k,v])=>`<div class="metric"><div class="metric-label">${k}</div><div class="metric-value">${Math.round(v.avg_score).toLocaleString()}</div><div class="metric-change">${v.count} clean records</div></div>`).join('');