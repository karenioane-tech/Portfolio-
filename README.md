
## Quick start

Each app is independent and can be run from its own folder:

```bash
cd personal_data_diary
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
python3 app.py
```

